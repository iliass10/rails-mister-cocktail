<div class="container">
  <h2>Voici vos <strong>Cocktails<strong>:</h2>
    <ul>
    <% @cocktails.each do |cocktail| %>
    <li><%= link_to cocktail.name, cocktail_path(cocktail)%></li>
    <% end %>
  </ul>
  <%= link_to "Add new cocktail", cocktails_new_path %>
</div>